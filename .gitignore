node_modules/
dist/
.astro/
.vercel/

.vercel

# Per-machine Claude Code permission grants. Rewritten on almost every
# session, so tracking it produces constant churn with no shared value.
# Shared/team settings belong in .claude/settings.json instead.
.claude/settings.local.json

# Remotion render output (large binaries; re-renderable from source)
video/out/

# impeccable skill output (timestamped critique runs; regenerable).
# Keep .impeccable/assets/ — it's project content, not output: PRODUCT.md
# > Conversion & proof directs client logos and testimonials there.
.impeccable/*
!.impeccable/assets/

# extract-design-system skill output (scraped tokens; keep out of the repo)
.extract-design-system/
# Ignore scraped design-system output, but keep hand-authored files tracked.
design-system/*
!design-system/brand-discrepancy.md
!design-system/tokens.css
